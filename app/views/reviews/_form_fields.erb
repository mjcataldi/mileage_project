  <input type="hidden" name="review[reviewer_id]" value="<%= @review.reviewer_id %>" />
  <input type="hidden" name="review[restaurant_id]" value="<%= @review.restaurant_id %>" />
  <input type="text" name="review[title]" placeholder="Enter the Review Title" value="<%= @review.title %>"/>
  <textarea name="review[body]" placeholder="Enter your review here" /><%= @review.body %></textarea>
  <input type="range" name="review[rating]" min="1" max="5" placeholder="Rating" value="<%= @review.rating %>"/>
